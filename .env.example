# Moldable Environment Configuration
# 
# Moldable uses a LAYERED environment variable system with workspace awareness:
# 1. Shared .env (~/.moldable/shared/.env) - Base values for ALL workspaces
# 2. Workspace .env (~/.moldable/workspaces/{id}/.env) - Overrides for specific workspace
#
# Variables in workspace .env files override shared values.
# This allows you to use different API keys per workspace (e.g., work vs personal).
#
# Quick setup:
#   mkdir -p ~/.moldable/shared
#   cp .env.example ~/.moldable/shared/.env
#   # Then edit ~/.moldable/shared/.env with your keys
#
# For workspace-specific overrides:
#   mkdir -p ~/.moldable/workspaces/{workspace-id}
#   cp .env.example ~/.moldable/workspaces/{workspace-id}/.env
#   # Add only the variables you want to override for this workspace

# ============================================
# LLM API Keys (at least one required)
# ============================================

# Open Router - https://openrouter.ai/keys
OPENROUTER_API_KEY= 

# Anthropic (Claude) - https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenAI - https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ============================================
# Optional: Web Search Tool
# ============================================

# Google Custom Search - https://developers.google.com/custom-search/v1/overview
# 1. Create a Custom Search Engine: https://cse.google.com/cse/
# 2. Get API key: https://console.cloud.google.com/apis/credentials
GOOGLE_SEARCH_ENGINE_API_KEY=
GOOGLE_SEARCH_ENGINE_ID=

# ============================================
# Optional: App-specific Keys
# ============================================

# DeepL Translation (for Scribo app) - https://www.deepl.com/en/your-account/keys
DEEPL_API_KEY=

# ============================================
# Optional: Server Configuration
# ============================================

# AI server port (default: 3100)
# MOLDABLE_AI_PORT=3100

# AI server host (default: 127.0.0.1)
# MOLDABLE_AI_HOST=127.0.0.1

# Override workspace root (default: auto-detected)
# MOLDABLE_WORKSPACE=/path/to/workspace

# Enable debug logging
# MOLDABLE_DEBUG=true

# ============================================
# Workspace-Specific Override Example
# ============================================
#
# To override API keys for a specific workspace (e.g., "work"),
# create ~/.moldable/workspaces/work/.env with:
#
#   # Override OpenAI key for work workspace
#   OPENAI_API_KEY=sk-work-...
#
#   # Add work-only keys
#   COMPANY_API_KEY=secret
#
# The workspace .env file only needs to contain variables you want
# to override or add - all other variables will be inherited from
# the shared .env file.
